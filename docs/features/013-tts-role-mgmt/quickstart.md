# Quickstart: TTS 角色管理介面

**Feature**: 013-tts-role-mgmt
**Date**: 2026-01-29

## 功能概述

TTS 角色管理介面讓你能夠：
- 為 TTS 角色設定自訂顯示名稱（如「Puck」→「陽光男孩聲」）
- 收藏常用角色，讓它們在選單中優先顯示
- 隱藏不需要的角色，簡化選擇介面

## 使用方式

### 1. 進入角色管理頁面

從側邊欄選擇 **設定** → **角色管理**，或直接訪問 `/voice-management`。

### 2. 瀏覽和篩選角色

使用頁面上方的篩選工具：
- **提供者**：選擇 VoAI、Gemini、Azure 等
- **語言**：篩選特定語言的角色
- **搜尋**：輸入關鍵字搜尋角色名稱
- **顯示隱藏**：切換是否顯示已隱藏的角色

### 3. 自訂角色名稱

1. 找到想要編輯的角色
2. 點擊角色名稱旁的編輯圖示 ✏️
3. 輸入新的顯示名稱（最多 50 字元）
4. 按 Enter 或點擊確認儲存

> 💡 清空名稱欄位將恢復使用原始名稱

### 4. 收藏角色

點擊角色行中的星星圖示 ⭐ 即可收藏或取消收藏。

收藏的角色會在 TTS 設定頁面的語音選單中優先顯示。

### 5. 隱藏角色

點擊角色行中的眼睛圖示 👁 即可隱藏或取消隱藏。

隱藏的角色不會出現在 TTS 設定頁面的語音選單中。

> ⚠️ 隱藏角色時會自動取消收藏狀態

## API 使用範例

### 列出所有角色（含自訂設定）

```bash
curl -X GET "http://localhost:8000/api/v1/voices?exclude_hidden=true" \
  -H "Authorization: Bearer <token>"
```

回應範例：
```json
{
  "items": [
    {
      "id": "gemini:Puck",
      "name": "Puck",
      "display_name": "陽光男孩聲",
      "provider": "gemini",
      "language": "zh-TW",
      "gender": "male",
      "is_favorite": true,
      "is_hidden": false
    }
  ],
  "total": 85,
  "limit": 100,
  "offset": 0
}
```

### 更新角色設定

```bash
curl -X PUT "http://localhost:8000/api/v1/voice-customizations/gemini:Puck" \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{
    "custom_name": "陽光男孩聲",
    "is_favorite": true,
    "is_hidden": false
  }'
```

### 批量更新多個角色

```bash
curl -X PATCH "http://localhost:8000/api/v1/voice-customizations/bulk" \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{
    "updates": [
      {"voice_cache_id": "gemini:Puck", "is_favorite": true},
      {"voice_cache_id": "gemini:Kore", "custom_name": "溫柔女聲"},
      {"voice_cache_id": "voai:tw-male-01", "is_hidden": true}
    ]
  }'
```

## 常見問題

### Q: 自訂設定會在角色同步時消失嗎？

不會。角色同步只會更新原始資料（VoiceCache），你的自訂設定（名稱、收藏、隱藏）儲存在獨立的表格中，不受同步影響。

### Q: 可以同時收藏和隱藏一個角色嗎？

不行。隱藏角色時會自動取消收藏狀態。這是因為隱藏的角色不會出現在選單中，收藏也就沒有意義。

### Q: 最多可以收藏多少個角色？

沒有限制。但建議只收藏真正常用的角色，以維持選單的簡潔性。

### Q: 自訂名稱有什麼限制？

最多 50 個字元。可以使用任何語言的文字、數字、符號和 emoji。

## 開發者資訊

### 資料庫結構

新增 `voice_customization` 表：

| 欄位 | 類型 | 說明 |
|------|------|------|
| id | SERIAL | 主鍵 |
| voice_cache_id | VARCHAR | 關聯 voice_cache.id |
| custom_name | VARCHAR(50) | 自訂名稱 |
| is_favorite | BOOLEAN | 收藏狀態 |
| is_hidden | BOOLEAN | 隱藏狀態 |
| created_at | TIMESTAMP | 建立時間 |
| updated_at | TIMESTAMP | 更新時間 |

### API 端點

| 方法 | 端點 | 說明 |
|------|------|------|
| GET | /api/v1/voice-customizations | 列出所有自訂設定 |
| GET | /api/v1/voice-customizations/{id} | 取得單一設定 |
| PUT | /api/v1/voice-customizations/{id} | 更新設定（upsert） |
| DELETE | /api/v1/voice-customizations/{id} | 重設為預設值 |
| PATCH | /api/v1/voice-customizations/bulk | 批量更新 |

### 前端元件

- `VoiceManagementPage` - 角色管理頁面
- `VoiceManagementTable` - 角色列表表格
- `VoiceCustomizationRow` - 單一角色行
- `VoiceNameEditor` - 名稱編輯器
- `FavoriteToggle` - 收藏切換
- `HiddenToggle` - 隱藏切換
- `VoiceFilters` - 篩選工具列
